syntax = "v1"

type (
	Response {
		Code int         `json:"code"`
		Msg  string      `json:"msg"`
		Data interface{} `json:"data"`
	}
)

@server(
	prefix: /api/v1/user
)

service user {
	@server(
		handler: Get
	)
	get /index returns (Response)
}